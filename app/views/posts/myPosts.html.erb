<div class="page-header">
  <% if user_signed_in? %>
      <%= link_to "New",new_post_path, :class=>"btn btn-success pull-right" %>
  <% end %>
  <h2>My Posts</h2>
</div>
<div id="posts">
  <%= render 'posts' %>
</div>
<div id="paginate">
  <%= render 'paginate' %>
</div>
