<%= form_for(likes_comments_by_user) do |f| %>
  <% if likes_comments_by_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(likes_comments_by_user.errors.count, "error") %> prohibited this likes_comments_by_user from being saved:</h2>

      <ul>
      <% likes_comments_by_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :is_possitive %>
    <%= f.check_box :is_possitive %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :comment_id %>
    <%= f.text_field :comment_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
